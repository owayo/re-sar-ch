# 変更履歴

バージョンは CalVer (`YY.M.PATCH`) で、番号からは破壊的変更かどうかが読み取れない。
**互換性を壊す変更は「⚠️ 破壊的変更」として各版の先頭に置く。**

## 未リリース

### ⚠️ 破壊的変更

- **`detect` の `text` を既定で要約にした。** エピソードを 1 件ずつ並べるのをやめ、
  **見出しの系列ごとにまとめて**「何が何件・いつ鳴ったか」を出す。検出 1 件ごとの内訳
  (系列・平均の取り方・条件の根拠文・観測値の列挙・比較基準の数値) と、検出パターンごとに
  固定の「考えられる解釈」も本文から外した。**従来どおりの全文は `--verbose`** で出る
  (出力は以前と 1 文字も変わらない)。`--format json` / `--format ndjson` は
  指定によらず全フィールドを出すので影響を受けない。
  実測で **1 日分の実ファイルが 3019 行 → 244 行** (111 エピソード / 23 系列)
- 落としたのは**繰り返しだけ**である。比較基準の出所・評価の網羅度・注意は要約でも出る。
  系列ごとのまとまりには、**その系列で鳴った時刻・回ごとの優先度・採取回数**が並び
  (8 件を超えたら件数を出して打ち切る)、**同時に別の系列が鳴ったエピソードは `+N 系列`**
  として残る。「この所見では確かめていないこと」はエピソード本文から外したが、
  **指標ごとに 1 度だけ末尾へ集約して必ず出す** (エピソードが 111 件あっても 1 度)。
  系列に固有の留保 (中央値そのものが固定条件の内側、基準が異変側へ寄っている疑い) も
  要約に残る

### ⚠️ 破壊的変更 (続き)

- **`detect` の出力言語を実行環境から決めるようにした。** これまでは常に日本語だった。
  **日本語のままにするには `--lang ja`** を付けるか、`LANG=ja_JP.UTF-8` のような
  ロケールを設定する。判定の順は
  `--lang` → `RESARCH_LANG` → `LC_ALL` / `LC_MESSAGES` / `LANG` → `LANGUAGE` →
  ローカルタイムゾーン (日本なら日本語) → 英語。
  **ロケール環境変数はタイムゾーンより強い** (ロケールは「どの言語で読みたいか」の
  宣言そのもので、タイムゾーンは「どこにいるか」でしかない)。
  `LC_ALL=C` は `LANGUAGE` より強く、英語で確定する (スクリプトが決定的な出力を得る慣行)
- **ライブラリとして使っている場合、表示文字列の API が変わった。** CLI だけを使うなら
  影響しない。`Priority::label` / `SufficiencyLevel::label` / `SufficiencyBasis::label` /
  `NotEvaluated::label` / `ObservationOrigin::label` / `MeanBasis::label` /
  `Pattern::label` / `DetectRoute::label` / `BasisOrigin::label` / `Dispersion::label` /
  `FixedComparison::label` は **`&'static str` ではなく `Text` (日英の対) を返す**。
  `.get(lang)` を挟めば従来の文字列が取れる。
  `SufficiencySpread::label` / `RouteStatus::label` / `ReportBoundary::label` /
  `ShiftDirection::as_str` / `TemporalSupport::describe_span` / `describe_duration` /
  `describe_detection` / `boundary_phrase` / `SeriesEvaluation::{observed, excluded, absent}`
  は **引数に `Lang` が増えた**。`CatalogEntry` の `label` / `interpretations` /
  `not_established` / `FixedCondition::rationale` も `Text` になった
- `DetectOptions` に `lang` フィールドが増えた (`..Default::default()` で組んでいれば
  英語になる)。`Assessment` は組み立てに使った言語を `lang` で持つ

### 追加

- **本家 `sar` が読む環境変数 2 つに対応した。** 互換出力
  (`sar` / `sa2sar` / `show --format sar`) だけに効く
  - `S_TIME_FORMAT=ISO` … バナー行の日付を `MM/DD/YY` から `YYYY-MM-DD` にする。
    本家と同じ完全一致判定なので `iso` のような綴りは効かない。
    時刻側は本家では `%X` → `%H:%M:%S` に変わるが、reSARch はロケールを持たず
    元から `%H:%M:%S` なので差が出ない
  - `S_REPEAT_HEADER=<N>` … 列見出しを出し直すまでの行数。
    **標準出力が端末でないときだけ**見られる (本家と同じ `else if`)。
    端末のときは代わりにウィンドウの高さ (`rows - 2`) を使う
  - `sadf` には効かない。本家が `sadf` で `S_F_PREFD_TIME_OUTPUT` を立てないため

- `detect --verbose` を追加した (上記の全文表示)
- **`detect` を日本語 / 英語の 2 言語に対応させた。** 指標名・検出の説明文・
  考えられる解釈・確かめていないこと・評価の網羅度・注意まで、
  `text` と SVG のすべてを訳してある。`--lang ja` / `--lang en` で明示指定できる
- `detect --format json` / `--format ndjson` のエピソードに `headline_series` と
  `headline_metric_label` を足した。どの検出が見出しになったかを、
  見出し文字列を解析せずに取れる
- **TUI に時系列グラフ。** 表の上に、選択中の activity / item / 列の 1 系列を折れ線で
  描く。線は不連続と欠測のところで切れ、飛び越えて結ばない (その間を観測していないため)。
  1 点も描けないときは空の軸ではなく理由を出す。表で選んでいる時刻には赤い縦線が立ち、
  グラフに出している列は表の見出しも線と同じ色になる
- **TUI の列操作。** `c` で表に出す列とグラフの列を選ぶ。全時刻で値が出なかった列は
  既定で表から外し、外した数をタイトルに出す (黙って消さない)。`[` / `]` でグラフの列を
  送る (表に出ている列だけを辿る)。`shift` + `←` / `→` で表を横に送り、
  タイトルに `9-18/23 列` と位置を出す
- TUI の列幅を、列名の長さではなく**全サンプルの値の桁**で決めるようにした。
  値が短い列が無駄に幅を取らなくなり、行をスクロールしても幅が揺れない

### 変更

- **TUI のキーから大文字を廃止した。** `g` / `G` → `home` / `end`、
  表の列選択 `C` はグラフの列選択 `c` に統合。`shift` の有無で別の操作になると、
  打ち間違いが「別の機能が動く」形で出るため
- TUI のグラフの X 軸の目盛りを、画面幅に入るだけ刻むようにした
  (表示範囲が 1 時間を超えたら秒を落として `HH:MM` にする)

### 修正

- **`sar` テキストの列見出しが再表示されていなかった。** `sar -A` / `sa2sar` の
  ような CPU ビットマップを使う出力では、本家は**既定のパイプ出力でも**
  一定行ごとに見出しを出し直す。reSARch はブロック先頭の 1 回しか出していなかった。
  **`sa2sar` / `sar -A` の出力に見出し行が増える** (実ファイル 29 本で 1 本あたり
  平均 26 行)。値・列・行順は変わらない
  - 原因は本家の `lines` の数え方。ビットマップを持つ activity
    (`A_CPU` / `A_IRQ` / `A_PWR_CPU` / `A_PWR_FREQ` / `A_NET_SOFT`) は
    **印字行数ではなく `count_bits(cpu_bitmap)` を足す**。`-P ALL` と `-A` は
    ビットマップ全体を `~0` で埋めるので、実 CPU 数ではなく常に 8200
    (`BITMAP_SIZE(NR_CPUS)` バイト × 8) になり、既定の 86400 行を 11 サンプルで
    越える。本家 sysstat が同じファイルから作ったテキストと**見出しの行番号が
    完全一致**することを実データで確認した
  - 併せて、全 item が `-z` や名前フィルタで落ちたレコードでも見出しは出すようにした
    (本家は item ループの前で見出しを書くため)。`Average:` ブロックの見出し有無も
    本家の `dish` の持ち越し規則に合わせた
- `sadf -g -O oneday` の 24 時間軸が `-T` / `-t` を無視して UTC 固定で切っていた。
  他のラベルと同じ時刻基準で切るようにした
- TUI のグラフが、その世代に無い列を初期選択して「描画できる観測がありません」に
  当たることがあった。値の出る最初の列を選ぶようにした

## v26.9.102

### ⚠️ 破壊的変更 — 独自サブコマンドの時刻表示が UTC からローカルタイムゾーンへ

`show` / `summarize` / `detect` / `compare` / `tui` の日時が、**実行環境の
ローカルタイムゾーン**で出るようになった (これまでは UTC 固定)。

```
旧: 起動区間 0  2019-06-30 05:39:33Z → 2019-06-30 05:39:34Z
新: 起動区間 0  2019-06-30 14:39:33+09:00 → 2019-06-30 14:39:34+09:00
```

**従来どおり UTC で出すには `--utc` (または `--timezone utc`) を付ける。**
テキスト出力を機械処理している箇所があれば、この指定が要る。

```bash
resarch summarize sa01 --utc            # 従来の …Z 表記
resarch detect sa01 --timezone utc
resarch show sa01 --timezone Asia/Tokyo # 実行環境によらず固定
```

同じ基準が `--from` / `--to` の `hh:mm[:ss]` の解釈にも効く。
`--from 09:00` は「実行環境のローカル 09:00」になった。
10 桁の epoch 秒で指定した場合はこれまでどおりタイムゾーンの影響を受けない。

**影響しないもの**:

- 互換出力 (`resarch sar` / `resarch sadf` / `resarch sa2sar`) — 本家 sysstat の
  規則のまま。全文比較テストも変わっていない
- 機械可読形式の epoch 秒 (`show --format json` / `ndjson` / `csv` の
  `start_epoch` / `end_epoch`)
- `info` / `identify` — ファイルヘッダに書かれた値をそのまま出すコマンド

### 追加

- **独自サブコマンドに `--timezone <local|utc|IANA名>`。** `--utc` はその別名で、
  同時指定はエラー。タイムゾーン名は IANA 名 (`Asia/Tokyo`) で出し、特定できない
  環境では数値オフセット (`+09:00`) になる
- `detect --format json` / `--format ndjson` に `report_timezone`。
  `--from` / `--to` をどの壁時計として読んだかを示す
