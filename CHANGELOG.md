# 変更履歴

バージョンは CalVer (`YY.M.PATCH`) で、番号からは破壊的変更かどうかが読み取れない。
**互換性を壊す変更は「⚠️ 破壊的変更」として各版の先頭に置く。**

## 未リリース

### ⚠️ 破壊的変更

- **`detect` の既定テキストを、優先度順の最大5系列・各3件の時刻に絞った。**
  代表例の観測値と判定理由を添え、背景の所見は優先度順に最大3件表示する。
  省略件数・比較基準の留保・評価不能理由は残す。全件の詳細は `--verbose`、
  全フィールドは `--format json` / `--format ndjson` で取得できる。
- `detect` の日本語を見直し、「根拠の充足度」を「判断に使えるデータ」、
  「評価の網羅度」を「評価できた範囲」と表記する。判定理由の長い文を分け、
  代表例にはその検出自体の時刻を添える。JSON のキーと判定条件は変えない。
  「最小有意変化量」も平易な表現に変更し、ディスク応答時間と受信パケット破棄の
  説明を短い文に分けた。観測値や閾値などの数値は保持する。

- ライブラリの `OpenOptions` に `legacy_endian`、`StructSource` に `PackedLegacy` を追加した。
  構造体を全フィールド指定で作る場合は `legacy_endian: None` または `..Default::default()` を加える。
  `StructSource` を網羅的に match している場合は新しい分岐を追加する。既存 CLI の指定方法は変わらない。

- **`detect` の `text` を既定で要約にした。** エピソードを 1 件ずつ並べるのをやめ、
  **見出しの系列ごとにまとめて**「何が何件・いつ鳴ったか」を出す。検出 1 件ごとの内訳
  (系列・平均の取り方・条件の根拠文・観測値の列挙・比較基準の数値) と、検出パターンごとに
  固定の「考えられる解釈」も本文から外した。**従来どおりの全文は `--verbose`** で出る
  (出力は以前と 1 文字も変わらない)。`--format json` / `--format ndjson` は
  指定によらず全フィールドを出すので影響を受けない。
  実測で **1 日分の実ファイルが 3019 行 → 244 行** (111 エピソード / 23 系列)
- 落としたのは**繰り返しだけ**である。比較基準の出所・評価の網羅度・注意は要約でも出る。
  系列ごとのまとまりには、**その系列で鳴った時刻・回ごとの優先度・採取回数**が並び
  (8 件を超えたら件数を出して打ち切る)、**同時に別の系列が鳴ったエピソードは `+N 系列`**
  として残る。「この所見では確かめていないこと」はエピソード本文から外したが、
  **指標ごとに 1 度だけ末尾へ集約して必ず出す** (エピソードが 111 件あっても 1 度)。
  系列に固有の留保 (中央値そのものが固定条件の内側、基準が異変側へ寄っている疑い) も
  要約に残る

### ⚠️ 破壊的変更 (続き)

- **`detect` の出力言語を実行環境から決めるようにした。** これまでは常に日本語だった。
  **日本語のままにするには `--lang ja`** を付けるか、`LANG=ja_JP.UTF-8` のような
  ロケールを設定する。判定の順は
  `--lang` → `RESARCH_LANG` → `LC_ALL` / `LC_MESSAGES` / `LANG` → `LANGUAGE` →
  ローカルタイムゾーン (日本なら日本語) → 英語。
  **ロケール環境変数はタイムゾーンより強い** (ロケールは「どの言語で読みたいか」の
  宣言そのもので、タイムゾーンは「どこにいるか」でしかない)。
  `LC_ALL=C` は `LANGUAGE` より強く、英語で確定する (スクリプトが決定的な出力を得る慣行)
- **ライブラリとして使っている場合、表示文字列の API が変わった。** CLI だけを使うなら
  影響しない。`Priority::label` / `SufficiencyLevel::label` / `SufficiencyBasis::label` /
  `NotEvaluated::label` / `ObservationOrigin::label` / `MeanBasis::label` /
  `Pattern::label` / `DetectRoute::label` / `BasisOrigin::label` / `Dispersion::label` /
  `FixedComparison::label` は **`&'static str` ではなく `Text` (日英の対) を返す**。
  `.get(lang)` を挟めば従来の文字列が取れる。
  `SufficiencySpread::label` / `RouteStatus::label` / `ReportBoundary::label` /
  `ShiftDirection::as_str` / `TemporalSupport::describe_span` / `describe_duration` /
  `describe_detection` / `boundary_phrase` / `SeriesEvaluation::{observed, excluded, absent}`
  は **引数に `Lang` が増えた**。`CatalogEntry` の `label` / `interpretations` /
  `not_established` / `FixedCondition::rationale` も `Text` になった
- `DetectOptions` に `lang` フィールドが増えた (`..Default::default()` で組んでいれば
  英語になる)。`Assessment` は組み立てに使った言語を `lang` で持つ

### 追加

- **`make sar-upstream-all` を追加した。** 公式 Git 133版と回収した旧ソース48版の
  計181ケースを固定 manifest で管理し、版ごとのイメージ作成・採取・同版 sar 描画・
  reSARch の EOF 検証と描画を自動実行する。ハッシュ検証、再開、失敗後の継続に対応。
  manifest 更新スクリプトで新規の公式リリースも追加できる。
  181件すべての実採取・末尾一致・描画を確認し、sa / 同版sarテキスト / 来歴 / SHA-256を
  Git管理に追加。収録した全181ファイルを通常の `cargo test` でも走査・デコードする。
  EOF一致だけで成功にせず、未知activityの読み飛ばしも失敗として検出する。

- **sysstat 11.7.1 のactivity magic更新漏れに対応した。** 原典と翌版を比較して確認した
  17種類を、生成版と形式が一致する場合だけ正しい配置へ対応付ける。独自出力・集計で
  統計を読み取れるようにし、現行sar互換出力は本家の表示規則を維持する。

- **登録済み28形式すべてに読み取り経路を追加した。** sysstat 6.1.1〜6.1.2 の
  `0x2168`、旧モノリシック22形式、sysstat 2.2 の選択列形式を含む。
  原典の配置を実測し、旧24リリースの実採取と両ABI・エンディアンの独立 fixture で検証。
  欠落・単位不明の値を取得不可にし、古いカーネルの PAGE / IO を現代の単位で誤表示しない。
  保存されないABI・時刻情報の仮定と、配列・指標の制限は `docs/format/09-legacy-generations.md`。
  原典未入手のリリースへの対応を保証するものではない。


- **CentOS 3.9／4.9 (`0x2163`)・5.11 (`0x2169`) の sa 読み取りに対応した。**
  モノリシック形式を直接デコードし、各採取ファイルの exact EOF と統計値を検証。
  既存の表示・集計経路を使い、未記録フィールドは取得不可として保持する。
  CPU 別 IRQ の旧配列と PID 付きパイプ入力はデコード対象外。
  `0x2163` の long 幅は既定 8 バイト（ライブラリの `OpenOptions.legacy_long_bytes` で 4 を指定可能）。
  自動採取でもこの 3 ケースの走査・描画を検証する。

- **`make sar-all` で記録済みの全20ケースを自動採取できるようにした。**
  既存5ディストリビューション・sysstat公式リリース4世代に、CentOS Vaultの11ケース
  (3.9〜8.5、6.5 / 7.5を含む) を追加。`make sar-centos` でCentOSのみ実行できる。
  `sa` / 本家 `sar` テキスト / 来歴 / SHA-256 / ケース別ログを保存し、
  採取失敗とreSARchの読み取り未対応を分けて一覧化する。
- **`--sar-profile sysstat-10.1.5-el7` で RHEL / CentOS 7 の `sar` の出力を再現できるようにした。**
  `sa2sar` と `sar` 互換入口で使える。RHEL 7 のホストの `sa2` が書いた `sarDD` と
  バイト単位で一致する (実ホストの 29 日分で確認)
  - これまでは常に本家 12.8.0 の書式で出していたため、CentOS 7 のレポートとは列
    (`-B` の `%vmeff`、`-d` の `rd_sec/s … svctm`、`-R` ブロックなど) も、
    値の求め方 (CPU `all` 行、平均の整数除算、オフライン CPU の扱い) も一致しなかった
  - **既定は変わらない** (`current` = `sysstat-12.8.0`)。版はファイルから自動では選ばない
  - 読めるのは `format_magic` 0x2171 のファイルだけ (本家 10.1.5 と同じ)
  - `sar` 互換入口では文法も el7 になる (`-h` はヘルプ、`-R` がある、`-I XALL` / 番号指定)。
    10.1.5 に無いオプションは usage エラー
  - `-R` のページ換算に使うページサイズは `--sar-page-size` (既定 4096) で指定する。
    ファイルに記録されない値のため
  - `sadf` はプロファイルを持たない。指定すると出力する前にエラーにする
  - 仕様と検証手順は `docs/format/05-sysstat-10.1.5-el7.md`

- **本家 `sar` が読む環境変数 2 つに対応した。** 互換出力
  (`sar` / `sa2sar` / `show --format sar`) だけに効く
  - `S_TIME_FORMAT=ISO` … バナー行の日付を `MM/DD/YY` から `YYYY-MM-DD` にする。
    本家と同じ完全一致判定なので `iso` のような綴りは効かない。
    時刻側は本家では `%X` → `%H:%M:%S` に変わるが、reSARch はロケールを持たず
    元から `%H:%M:%S` なので差が出ない
  - `S_REPEAT_HEADER=<N>` … 列見出しを出し直すまでの行数。
    **標準出力が端末でないときだけ**見られる (本家と同じ `else if`)。
    端末のときは代わりにウィンドウの高さ (`rows - 2`) を使う
  - `sadf` には効かない。本家が `sadf` で `S_F_PREFD_TIME_OUTPUT` を立てないため

- `detect --verbose` を追加した (上記の全文表示)
- **`detect` を日本語 / 英語の 2 言語に対応させた。** 指標名・検出の説明文・
  考えられる解釈・確かめていないこと・評価の網羅度・注意まで、
  `text` と SVG のすべてを訳してある。`--lang ja` / `--lang en` で明示指定できる
- `detect --format json` / `--format ndjson` のエピソードに `headline_series` と
  `headline_metric_label` を足した。どの検出が見出しになったかを、
  見出し文字列を解析せずに取れる
- **TUI に時系列グラフ。** 表の上に、選択中の activity / item / 列の 1 系列を折れ線で
  描く。線は不連続と欠測のところで切れ、飛び越えて結ばない (その間を観測していないため)。
  1 点も描けないときは空の軸ではなく理由を出す。表で選んでいる時刻には赤い縦線が立ち、
  グラフに出している列は表の見出しも線と同じ色になる
- **TUI の列操作。** `c` で表に出す列とグラフの列を選ぶ。全時刻で値が出なかった列は
  既定で表から外し、外した数をタイトルに出す (黙って消さない)。`[` / `]` でグラフの列を
  送る (表に出ている列だけを辿る)。`shift` + `←` / `→` で表を横に送り、
  タイトルに `9-18/23 列` と位置を出す
- TUI の列幅を、列名の長さではなく**全サンプルの値の桁**で決めるようにした。
  値が短い列が無駄に幅を取らなくなり、行をスクロールしても幅が揺れない

### 変更

- **TUI のキーから大文字を廃止した。** `g` / `G` → `home` / `end`、
  表の列選択 `C` はグラフの列選択 `c` に統合。`shift` の有無で別の操作になると、
  打ち間違いが「別の機能が動く」形で出るため
- TUI のグラフの X 軸の目盛りを、画面幅に入るだけ刻むようにした
  (表示範囲が 1 時間を超えたら秒を落として `HH:MM` にする)
- Windows 版の `skill-install` が書き出す `SKILL.md` の改行を、ほかの OS と同じ LF にした
  (これまでは CRLF)。Claude Code / Codex の読み込みには影響しない
- ソースから入れたときの `make uninstall` は、`INSTALL_PATH` のバイナリだけを消すようにした。
  `make install` が書き出したスキル (`~/.claude/skills/resarch` など) は残るので、
  要らなければ手で消す。スキルの置き場所はエージェントごとに違い、別の方法で入れた
  reSARch のスキルまで消してしまうため

### 修正

- `extra_desc` の本体が途中で終わった場合、切り詰めエラーに実際に残っている本体の
  バイト数を表示する。以前は残りがあっても常に 0 と表示していた。

- `detect` でデバイスの消失・再出現による系列内の観測の穴が、無関係な系列の
  エピソードまで分割していた。全系列に及ぶ不連続は、再起動などの根拠がある場合に限定した。
- `detect` の報告範囲で epoch 秒と日内時刻を混在させた場合の範囲外判定と、
  夏時間の切り替えで存在しない時刻・繰り返す時刻を含む区間の重なり判定を修正した。
- `detect --baseline-scope window` で、範囲外に採取された瞬時値が比較基準に混ざらないようにした。
  判定手順の変更に伴い、検出器の版を `resarch-detect/2` に更新した。

- **`compare --host NAME=DIR` が複数ホストのデータから先頭だけを選び、残りを黙って捨てていた。**
  ホスト名・OS・アーキテクチャの異なるグループが混在する場合はエラーにし、
  ホストごとに入力を分けるよう案内する。
- **`compare` の JSON / NDJSON に、比較した起動区間と異なるカーネル版・CPU 数が出ることがあった。**
  カーネル更新や CPU 数の変更を含む入力では、グループの先頭ファイルではなく、
  選択した起動区間のファイルから識別情報を取るようにした。

- **`sar -d` / `sadf` の `%util` と `aqu-sz` が、本家と丸め境界で 1 桁ずれることがあった。**
  保存単位から表示単位へ縮める係数を掛け算 (`× 0.1`、`× 0.001`) で当てていたため。
  本家と同じ割り算にした (実データで `%util` が本家 1.28 に対し 1.29 と出ていた)

- **`sar` テキストの列見出しが再表示されていなかった。** `sar -A` / `sa2sar` の
  ような CPU ビットマップを使う出力では、本家は**既定のパイプ出力でも**
  一定行ごとに見出しを出し直す。reSARch はブロック先頭の 1 回しか出していなかった。
  **`sa2sar` / `sar -A` の出力に見出し行が増える** (実ファイル 29 本で 1 本あたり
  平均 26 行)。値・列・行順は変わらない
  - 原因は本家の `lines` の数え方。ビットマップを持つ activity
    (`A_CPU` / `A_IRQ` / `A_PWR_CPU` / `A_PWR_FREQ` / `A_NET_SOFT`) は
    **印字行数ではなく `count_bits(cpu_bitmap)` を足す**。`-P ALL` と `-A` は
    ビットマップ全体を `~0` で埋めるので、実 CPU 数ではなく常に 8200
    (`BITMAP_SIZE(NR_CPUS)` バイト × 8) になり、既定の 86400 行を 11 サンプルで
    越える。本家 sysstat が同じファイルから作ったテキストと**見出しの行番号が
    完全一致**することを実データで確認した
  - 併せて、全 item が `-z` や名前フィルタで落ちたレコードでも見出しは出すようにした
    (本家は item ループの前で見出しを書くため)。`Average:` ブロックの見出し有無も
    本家の `dish` の持ち越し規則に合わせた
- **UTF-8 として読めない引数を渡すと panic していた** (終了コード 101)。
  Linux のファイル名はバイト列なので、そうしたパスも実在しうる。
  独自サブコマンド (`info` / `show` / `summarize` / `detect` など) はそのまま読めるようにし、
  引数を文字列として解析する `sar` / `sadf` 互換入口ではエラーメッセージを出して
  終了コード 1 で止まるようにした
- `sadf -g -O oneday` の 24 時間軸が `-T` / `-t` を無視して UTC 固定で切っていた。
  他のラベルと同じ時刻基準で切るようにした
- TUI のグラフが、その世代に無い列を初期選択して「描画できる観測がありません」に
  当たることがあった。値の出る最初の列を選ぶようにした
- `sadf -c` の警告 2 つが事実と違うことを書いていたので直した。変換結果は変わらない
  - レコード内件数 0 を書いたときの警告が「reSARch の読み取り側は 0 を拒否する」と
    書いていた。実際には reSARch も本家 `sar` もそのまま読める
  - 構造を解釈できない activity を素通ししたときの警告が、どの場合も
    「本家 `sadf -c` は exit 1 で中断する」と書いていた。中断するのは未知の ID だけで、
    既知の ID の未知 magic では本家は中断せずに変換を続ける
- 64bit の Windows で `S_REPEAT_HEADER` に `int` を超える値 (`2147483648` など) を
  与えると、本家 (64bit の Linux) と違う間隔で列見出しを出し直していた。
  C の `long` の幅を実行環境の ABI から取っていたため (Windows では 64bit でも 32bit)。
  本家が動く Linux と同じく、ポインタと同じ幅で読むようにした
- Windows で互換出力の「読み手のローカル時刻」が `TZ` を無視していた。`-t` を付けない
  `sar` の時刻とバナーの日付、`-s` / `-e` の比較、`sadf -T` の時刻、`sadf -g -O oneday` の
  日の区切りが、`TZ=America/New_York` のような指定があっても OS のタイムゾーンで出ていた。
  Windows の `chrono::Local` が `TZ` を読まないため。`TZ` が IANA 名ならそのタイムゾーンで
  開くようにし、Linux / macOS (本家の `localtime()` と同じく `TZ` に従う) と同じ時刻にした。
  `TZ` が無いときは従来どおり OS のタイムゾーンを使う。Linux / macOS の出力は変わらない

## v26.9.102

### ⚠️ 破壊的変更 — 独自サブコマンドの時刻表示が UTC からローカルタイムゾーンへ

`show` / `summarize` / `detect` / `compare` / `tui` の日時が、**実行環境の
ローカルタイムゾーン**で出るようになった (これまでは UTC 固定)。

```
旧: 起動区間 0  2019-06-30 05:39:33Z → 2019-06-30 05:39:34Z
新: 起動区間 0  2019-06-30 14:39:33+09:00 → 2019-06-30 14:39:34+09:00
```

**従来どおり UTC で出すには `--utc` (または `--timezone utc`) を付ける。**
テキスト出力を機械処理している箇所があれば、この指定が要る。

```bash
resarch summarize sa01 --utc            # 従来の …Z 表記
resarch detect sa01 --timezone utc
resarch show sa01 --timezone Asia/Tokyo # 実行環境によらず固定
```

同じ基準が `--from` / `--to` の `hh:mm[:ss]` の解釈にも効く。
`--from 09:00` は「実行環境のローカル 09:00」になった。
10 桁の epoch 秒で指定した場合はこれまでどおりタイムゾーンの影響を受けない。

**影響しないもの**:

- 互換出力 (`resarch sar` / `resarch sadf` / `resarch sa2sar`) — 本家 sysstat の
  規則のまま。全文比較テストも変わっていない
- 機械可読形式の epoch 秒 (`show --format json` / `ndjson` / `csv` の
  `start_epoch` / `end_epoch`)
- `info` / `identify` — ファイルヘッダに書かれた値をそのまま出すコマンド

### 追加

- **独自サブコマンドに `--timezone <local|utc|IANA名>`。** `--utc` はその別名で、
  同時指定はエラー。タイムゾーン名は IANA 名 (`Asia/Tokyo`) で出し、特定できない
  環境では数値オフセット (`+09:00`) になる
- `detect --format json` / `--format ndjson` に `report_timezone`。
  `--from` / `--to` をどの壁時計として読んだかを示す
